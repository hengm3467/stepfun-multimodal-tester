# Optional. Users can also paste their API key in the web UI.
STEPFUN_API_KEY=sk-your-stepfun-api-key
# Compatibility fallback if your environment already uses this name:
# STEPFUN_APP_ID=sk-your-stepfun-api-key
PORT=8787
